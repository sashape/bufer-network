name: release

# При пуше тега v* собираем exe и создаём GitHub Release с ним
on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Установка зависимостей
        run: pip install -r requirements.txt pyinstaller

      - name: Сборка exe
        run: pyinstaller --noconsole --onefile --name BuferNet --hidden-import pystray._win32 --exclude-module numpy --collect-all sv_ttk main.py

      - name: Релиз с exe
        run: gh release create ${{ github.ref_name }} dist/BuferNet.exe --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
